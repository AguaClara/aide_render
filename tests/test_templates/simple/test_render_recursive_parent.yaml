{% set parent = render_constants("test_render_recursive_parent.yaml") %}
{% set see = render_constants("test_render_recursive_child.yaml") %}
{% set self = see %}
{% import "test_render_recursive_child.yaml" as poop with context %}
parent_variable: "this is a variable defined in the parent"
child:
  {{ poop.child(self, parent) }}
{% set file_path = "/Users/ethankeller/git_repos/AguaClara/AIDE/aide_render/tests/test_templates/test_render_block.yaml" %}

total_height: 50 meter
n_layers: 10
side_length: {% set side_length = total_height/n_layers %}{{ side_length }}
# side_length: total_height/10 - haven't yet gotten to complete this. Maybe it's best to make a macro?

{% set block=render(open(file_path, 'r'), {"side_length": side_length}) %}

block: {{ block }}